;;; setup.lisp
;;;
;;; Workspace setup for demo.access. This MVP keeps setup minimal: the catalog
;;; data lives in catalog.lisp (read by Philo at runtime), so no APL workspace
;;; or meta-templates are needed here yet.

(in-package #:demo.access)
