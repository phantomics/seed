# portal.model.minimal

This is a barebones implementation of a Seed portal to illustrate basic portal functions. Here is an example of using the `(grow)` function:

```lisp
* (grow :portal.model.moss :view nil)
"hello"
```